const members=[1,2,3,4,5,6].map(n=>({name:`Member Company ${String(n).padStart(2,'0')}`,location:'Mauritius',specialisation:'Imported motor vehicles'}));const grid=document.getElementById('grid');const search=document.getElementById('search');const none=document.getElementById('none');const count=document.getElementById('count');function initials(name){return name.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase()}function render(list){grid.innerHTML=list.map(m=>`<article class="member"><div class="initial">${initials(m.name)}</div><h3>${m.name}</h3><p>${m.location}</p><p>${m.specialisation}</p></article>`).join('');none.hidden=list.length!==0;count.textContent=list.length}render(members);search.addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();render(members.filter(m=>Object.values(m).join(' ').toLowerCase().includes(q)))});document.getElementById('year').textContent=new Date().getFullYear();const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));